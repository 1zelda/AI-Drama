# 2026-09-25

## AI漫剧真人剧 工作区上传 GitHub（完成）

- 仓库：https://github.com/1zelda/AI-Drama （Private），推送成功 5d49314..7383c5d
- 内容：整个工作区（17 个 zip + 脚本/文档，约 230MB），OpenMontage.zip 73.7MB / director_ai.zip 67.8MB 触发 GitHub 50MB 警告但 <100MB 硬限，可正常推送
- 已合并远端自动生成的 README.md（--allow-unrelated-histories）
- .gitignore：排除 _*.txt 诊断日志、`'` 垃圾文件；_init/_commit/_push_log.txt 已从索引移除

### 重大坑：WorkBuddy 沙箱下 git 的 .git 目录不持久化
- **前台（沙箱内）命令创建的 .git 在命令结束后被丢弃**——init/commit 成功、下一条命令就 "not a git repository"，且无报错提示
- **后台任务（run_in_background）里创建的 .git 能持久化**（本次验证）
- 对策：把「init→config→add→commit→fetch→merge→push」全部塞进**一条命令**一次跑完；即使本地 .git 丢失，远端也已收到数据
- 中文路径注意：Set-Location 用 -LiteralPath；git 输出路径乱码是显示层 GBK/UTF-8 问题，不影响功能
- git config 代理 127.0.0.1:7890 曾报 TLS connect error（代理断网期间），网络恢复后正常
- 凭证：Git Credential Manager (credential.helper=manager) 已配好，push 免弹窗直通

## 垃圾清理 + .workbuddy 入库（4517037，完成）

- 逐个查看内容后删除 12 个测试垃圾：test*.txt×3、t.py、gen.js、gen_html.js、`'`(0字节)、gen_b64.txt（=build_libs.py 的 base64 重复）、4 个诊断日志
- 保留灰色文件：gen_all2.py / fix_new.py / build_libs.py（指向老路径 Documents\ChatGPT\... 的 ai-drama-studio 脚手架，是重建部分源码的唯一记录）、j1.b64 / js1.b64
- .workbuddy/memory/ 已入库推送；远端仓库与本地完全同步
