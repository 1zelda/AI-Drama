# 流程优化点 + 技能安装清单（2026-09-20）

诊断对象：`ai-drama-studio`（`drama-pro` v3 流水线）
依据：诊断书（9/02）、升级方案 v2、项目记忆 9/15→9/19、引擎与工作流源码复核

---

## 一、已经修好的（先确认，别重复做）

| 项 | 状态 |
|---|---|
| `image` 节点类型未注册 | ✅ `engine.py` 已是真实节点类型 |
| `execution.retry` / `timeout` 死配置 | ✅ 已生效，指数退避 + `_is_permanent()` 跳过无意义重试 |
| 竖屏成片（首帧被远程 URL 顶掉） | ✅ 9/19 修完，实测 1024×1792 → final 720×1280 |
| 镜头时长 vs 配音时长 | ✅ `postprod.py` 已做「配音长→冻结尾帧，短→截断」 |
| 字幕/配音/转场/BGM/片头片尾 | ✅ `postprod.py` 完整 |
| 剪映草稿导出 | ✅ `api/export_jianying.py` 已存在 |
| ffmpeg PIPE 的 gbk 崩溃 | ✅ 已改临时文件 |

**结论：端到端已真跑通，现在该做的是「稳定性 + 质量天花板 + 操作门控」，不是补路。**

---

## 二、还需要优化的（按「操作合理性」排序）

### P0 — 不改的话，长跑一定会踩

**1. run 状态只在内存里**
后端重启后 `/api/runs/{id}` 直接 404（9/19 已踩）。一集 30 镜 ≈ 40 分钟，跑完必须立刻抓结果，否则白跑。
→ 做法：新增 `backend/data/runs/<run_id>.json`，节点每完成一个就增量落盘；`/api/runs` 启动时 rehydrate。只加一个 storage 模块，不动引擎主循环。

**2. 没有断点续跑，一镜失败 = 整条重跑**
现在只有 retry（重试 2 次），重试不过整条 run 失败。30 镜里第 28 镜挂了，前 27 镜的图/视频全废。
→ 做法：镜头级 checkpoint `shots/<idx>.json`（记录 prompt/产物路径/状态），重跑带 `--resume` 只补缺失项；生图节点开 `continue_on_error`，跑完给「失败镜头清单」逐条补。

**3. 后端进程会无声退出**
9/19 记录「无 traceback，偶发退出」。长任务跑到一半静默死掉，前端没有任何提示。
→ 做法：`cmd /c start /min` 启动 + 每 30s 写 `data/heartbeat.txt`；前端 health 探活失败时显式提示「后端已掉线，点此重启」，而不是让用户对着空白页等。

### P1 — 质量天花板（漫剧能不能看，全在这三条）

**4. 角色一致性：已有管理器，但没接进流水线（最大缺口）**
`providers/character_manager.py` 有完整的角色档案 + `reference_images`，引擎 `_execute_image` 也支持 `reference_image(s)`，
**但 `drama-pro.json` 的 `shot_images` 节点没有 reference 字段** —— 现在每镜只靠文字描述，跨镜换脸/换装是必然的。
→ 做法：storyboard 每镜打 `characters:[id]` → image 节点按 id 取角色参考图填 `reference_images`；
云端图供应商若无图生图能力，就走「先出角色三视图资产 → 提示词锚定 + 参考图组图」。

**5. 镜头之间没有首尾帧续接**
现在每镜独立用本镜首帧生成，同场景相邻两镜容易跳。
→ 做法：同场景内，上一镜尾帧作为下一镜的额外参考；接口层已有 `tail_image_url` 类似能力的供应商优先。

**6. 全链没有质检门**
生图崩坏、人物走形、内容审核被拒，全靠人眼最后看。
→ 做法：生图后加可选 `qc` 节点（VLM 打分 < 阈值自动重写提示词重生成一次）；审核被拒走「提示词中和 + 重试」。成本敏感时只对人物关键镜开启。

### P2 — 操作体验与成本

**7. 并行度**：`execution.parallel: false`，而生图与 TTS 之间没有依赖 → 建议生图/TTS 并发 2–3，视频生成仍串行（供应商限流）。

**8. 提交前给预估**：一集跑多久、多少钱，提交前就该看到，而不是跑完才知道。

**9. 成片出口改成剪映草稿优先**：平台内硬调字幕/转场性价比低，`export_jianying.py` 已有，做成默认出口，人工精剪再回导。

**10. 工作区卫生**（最直观的一项）
根目录 26 个目录 + 12 个 zip 并存，8 个是空壳解压残留；`backend/` 下 `venv`（坏，指向旧机器 Python）和 `venv_broken` 两份；
`run1~run5.log / zp.log / _out.log / _err.log / *.b64 / test*.txt / gen*.js` 全是调试残留。
→ 建议：`_archive/` 收 zip 与一次性脚本；`_upgrade/`（26 个参考仓库）保留；确认后删 `venv_broken` 与日志残留；`backend` 单独 .gitignore。

---

## 三、建议的操作顺序（现在缺的就是「门控」）

```
创意/剧本 → 分集
  ↓ 【门1：资产确认】角色三视图 + 场景 —— 先出候选，人工选优（比直接批量生图省 90% 成本）
分镜 → 冻结关键帧
  ↓ 【门2：分镜确认】逐镜可改提示词
批量生图（带 QC，失败清单补跑）
  ↓
配音（多角色）→ 生视频（首尾帧续接）→ 合成 → 导出剪映草稿 → 人工精剪
```

`drama-pro` 现在是「一键到底」，中间没有门 1 / 门 2。**建议保留一键，但把两个门做成默认开启的开关**——这才是操作上合理的形态：全自动能跑，关键节点能拦。

真人剧路线额外需要：多角色 TTS（现在只有 edge-tts 单音色旁白）+ 口型（LatentSync / MuseTalk）。漫剧不需要口型，别提前做。

---

## 四、已安装技能（42 个，装在 `C:\Users\Administrator\.workbuddy\skills\`）

### 短剧全流程 / 制片厂
`ai-hive-short-drama-studio`（一站式制片厂）、`ai-short-drama-director`、`ai-short-drama-writer`、`short-drama-writer`、`short-drama-pro`、`generate-drama`、`jd-drama`（简丹短剧 CLI）、`drama-skills`、`meta-short-drama`、`kelly-drama`、`jackyshen-gen-short-video-script`、`fanqie-ai-video-drama`（番茄小说批量）、`ai-director`、`video-generation`（Sora2/Veo3/Seedance/Runway）

### 漫剧 / 动画
`ai-hive-comic-drama-factory`（动态漫画工厂）、`ai-model-expert-drama-ai-comic-drama`（生成与编辑）、`ai-model-expert-drama-comic-drama-editing`（剪辑）、`comic-drama-generate`、`aigc-ip-expert`（原创 IP 漫剧）、`aigc-cat-comic-review`（改编审核）、
`manju-*` 全套（writer / artist / director / animator / editor / workflow / factory）、`anime-drama`、`anime-drama-studio`、`anime-manga-generator`、`seedance-anime-drama`、`seedance-short-drama`、`ym-anime-character-sheet`、`ym-anime-plot-outline`

### 环节专项
`short-drama-storyboard-pipeline`（分镜提示词流水线）、`ai-short-drama-shot-maker`、`ai-comic-drama-shot-maker`（镜头）、`short-drama-voice-pack`（配音）、`short-drama-score-pack`（配乐）、`short-drama-emotion-curve`（情绪曲线）、`anime-storyboard-creator`、`storyboard-prompt-generator`

### 之前 IDE 里已有的（不用装，直接可用）
`short-drama` 整套（develop / write / assets / storyboard / video-prompts / produce / review / novel-analyze）、`ai-short-drama`、`多模态内容生成`

### 没装上的
`skills.volces.com@short-drama-pro`（火山引擎官方版）—— 安装器要求交互授权，非交互环境装不了。需要的话我换个方式单独拉。

---

## 五、下一步建议（按性价比）

1. run 状态落盘 + 断点续跑（半天，收益最大，长跑不再白跑）
2. `shot_images` 接上 `character_manager` 参考图（半天，直接决定跨镜一致性）
3. 生图后 QC 门 + 失败清单补跑（一天）
4. 工作区清理（半小时）

---

## 六、实施记录（当日已完成 1、2）

### ① run 状态落盘 + 断点续跑

- 新增 `backend/app/services/run_store.py`
  - `data/runs/<run_id>.json`：run 元信息 + 每个节点的状态/输出（节点完成即增量写）
  - `data/runs/<run_id>/shots/<node>/<idx>.json`：镜头级 checkpoint
  - 写入一律「临时文件 + `os.replace`」，进程被杀不会留下半个坏 JSON
- `engine.py`
  - `__init__` 新增 `store` / `resume`
  - 节点完成或失败立刻 `_persist_node()`（落盘失败不阻断主流程）
  - foreach 预分配 results，续跑时先把跑过的项回填，只跑剩下的（顺序严格对齐 index）
  - `_checkpoint_usable()`：checkpoint 里的产物文件已被清理 → 判定不可用，重跑这一镜
- `api/runs.py`
  - `POST /api/runs/{run_id}/resume`：接着跑没跑完的镜头；可带 `reset:["node_id"]` 强制重跑某节点
  - `GET /api/runs/{id}`、`/artifacts`、`GET /api/runs` 内存里没有就从磁盘恢复 → **重启不再 404**
  - 取消时保留 checkpoint，之后可续跑
- 前端 `/make`：runId 存 sessionStorage（刷新/后端重启后还能续），新增「断点续跑」按钮

### ② 角色一致性接进流水线

- image 节点新增 `character_refs` / `character_refs_max`，`drama-pro.json` 的 `shot_images` 已打开
- `_apply_character_refs()`：
  - 参考图来自角色档案库（CharacterManager），形象锚点来自上游 `character_generator`
  - 即使一张参考图都没有，也保证跨镜锚点文本逐字一致（以前只靠分镜师自觉复制）
  - 角色匹配：优先分镜里的 `characters`；没写就在镜头文本里做名字兜底匹配（老分镜也受益）
  - 只加不覆盖：工作流里手写的 `reference_image` 优先级更高
- `storyboard.yaml` 每镜新增 `characters` 字段

### 验证结果

| 检查 | 结果 |
|---|---|
| `py_compile` 三个文件 | 通过 |
| `from app.main import app` | 通过，路由含 `/api/runs/{run_id}/resume` |
| checkpoint 文件被删 | 判定不可用 → 重跑该镜 |
| 角色锚点 | 显式 characters 命中「林晚」；兜底名字匹配命中「顾沉舟」；空镜头不被污染 |
| 集成测试（假 provider） | 3 镜第 3 镜挂掉 → 落盘 `[0,1]`；续跑 llm 复用 0 次调用、只补 1 镜，顺序正确 |
| 前端 `tsc --noEmit` | 无类型错误 |

### 剩下没做的

- 生图 QC 门（VLM 打分 + 低分重写提示词重生成）
- 首尾帧续接（上一镜尾帧 → 下一镜参考）
- 并行度（生图/TTS 可并发，视频仍串行）
- 后端守护进程与心跳
- 工作区清理

---

## 七、实施记录（第二批：③④⑤⑥⑦ 全部完成）

### ③ 后端心跳 + 前端探活
- 引擎起心跳协程（`execution.heartbeat_interval`，默认 10s），持续发 `heartbeat` 事件
- 新增 `GET /api/system/ping`（极轻量，不做 provider 自检，供高频探活）
- 前端看门狗：静默 > 45s 就探活一次
  - ping 通 → 提示「供应商在处理中，不用中断」（只提示一次，不刷屏）
  - ping 不通 → 判定后端已死，停转并提示「点断点续跑接着跑」

### ④ 生图 QC 门（质检 + 自动改写重生成）
- image 节点新增 `qc` 配置：`enabled / threshold / max_attempts / prompt_template / model`
- `planner.vision_completion()`：多模态调用，本地图转 data URL；默认走智谱 `glm-4v-flash`（免费档），
  也可配 `VISION_API_KEY` / `VISION_BASE_URL` / `VISION_MODEL` 指向任意 OpenAI 兼容 VLM
- 打分低于阈值 → 用 VLM 给的 `rewrite_prompt` 重生成，最多 `max_attempts` 次；
  **全都没过阈值时保留得分最高的那张**（重写也可能越写越差）
- VLM 没配/调用失败 → 直接放行，质检绝不阻断生图
- 模板 `config/prompts/scripts/qc_vision.yaml`：5 个维度（完整性/吻合度/构图/风格/可用性）
- `drama-pro.json` 的 `shot_images` 已开启（阈值 6，最多 2 次）

### ⑤ 镜头首尾帧续接
- video 节点新增 `tail_frame_from_prev`：抽上一镜视频最后一帧作本镜**参考图**
- 本镜首帧仍是分镜图（构图由分镜决定），尾帧只延续人物状态/光影/色调 ——
  直接拿尾帧当首帧会把构图锁死成上一镜，镜头就死了
- `postprod.extract_tail_frame()`：ffmpeg `-sseof` 取尾帧，极短视频退化成取首帧
- 开了续接的节点自动降级为串行（并发时拿不到上一项产物）

### ⑥ 并发
- 节点级 `concurrency`（执行块里也有默认值），foreach 用信号量并发，结果仍按 index 对齐
- `drama-pro.json`：`shot_images` 和 `narration` 并发 2；视频保持串行（供应商限流）

### ⑦ 关键帧预览门（操作合理性最关键的一条）
- 节点级 `preview_gate: N` → 首次只生成前 N 项就停，状态 `waiting_approval`，
  **不带半成品往下跑**；用户确认后 `resume` 继续（复用已有 checkpoint，已出的不重跑）
- 前端：勾选「先出 2 张候选关键帧，确认后再批量生图」（默认开），
  收到 `node_waiting_approval` → 拉产物展示候选 + 「确认并继续」按钮
- run 状态同步为 `waiting_approval`，状态页不会误报「已完成」

### 验证结果（第二批）

| 检查 | 结果 |
|---|---|
| 预览门 | 首轮出 2 张 → `waiting_approval`、下游不跑；续跑补到 4 张，顺序 `[0,1,2,3]`，下游拿到 4 |
| 并发 | 4 项 ×0.3s：串行 1.2s → 实测 0.61s，峰值并发 3，顺序正确 |
| 心跳 | 短跑收到 6 条 heartbeat 事件 |
| 质检门 | 3 分 → 按建议重生成 → 8 分；VLM 不可用时放行不阻断 |
| 尾帧抽取 | ffmpeg 抽帧成功生成 png |
| `py_compile` / `app.main` 导入 | 通过，路由含 `/api/system/ping`、`/api/runs/{run_id}/resume` |
| 前端 `tsc --noEmit` | 无类型错误 |

### 现在还剩下的（都不紧急）
- 生图并发可调更高（供应商限流允许的话）
- QC 门的低分镜头汇总成「失败清单」，人工一次性补跑
- 后端守护进程（supervisor/服务化）+ 开机自启
- 工作区清理（26 个目录 / 12 个 zip、venv_broken、日志残留）—— 破坏性操作，需你确认清单
