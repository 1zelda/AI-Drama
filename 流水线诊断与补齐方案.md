# AI 漫剧流水线诊断与补齐方案

调研日期：2026-09-02　|　诊断对象：`ai-drama-studio`（工作区内你自己的项目）
工作区共 26 个目录 / 12 个 zip，其中 17 个为有效项目，8 个是空壳解压残留。

---

## 零、三个必须先知道的结论

### 结论 1：本机跑不了 ComfyUI，这是硬约束

```
GPU: GeForce MX110, 2048 MiB VRAM, driver 462.59
CPU: 8 核　|　E 盘剩余 46 GB
```

MX110 是 2018 年的入门独显，2 GB 显存。作为对照：

| 模型 | 最低显存 | 本机 |
|---|---|---|
| SD 1.5（512×512） | ~4 GB | 勉强 |
| SDXL | ~8 GB | 不可能 |
| Flux.1 / Flux2 | 12–24 GB | 不可能 |
| Wan 2.2 I2V 14B fp8 | 16–24 GB | 不可能 |

**所以「把 ComfyUI 装在本机」这条路是死的。** 但 ComfyUI 依然应该接入——只是作为**远程节点**（云 GPU 主机 / RunningHub / ComfyUI Cloud / 另一台有卡的机器），本机只做编排、提示词、资产管理、进度监控。这恰好跟你「可控可视可修改可操控」的诉求完全吻合：这些全是 CPU 侧的能力。

### 结论 2：默认工作流 `standard-drama.json` 跑不通，两处即炸

`backend/config/workflows/standard-drama.json` 是全项目唯一的端到端工作流定义，但：

```json
"image_gen": { "type": "image", "provider": "kling", ... }   // ← 炸点 1
"video_gen": { "type": "video", "provider": "seedance", ... } // ← 炸点 2
```

- **炸点 1**：引擎 `orchestrator/engine.py:150-160` 的 handler 表只注册了
  `llm / comfyui / comfyui_image / video / agnes_video / ffmpeg` 六种。
  没有 `image`。`type: "image"` 会走到 `:163` 抛
  `ValueError: Unknown node type 'image' on node 'image_gen'`。
- **炸点 2**：`providers/video_providers.py:220-221` 把 `seedance` 和 `veo`
  映射到 `NotImplementedProvider`，`:208` 直接 `raise NotImplementedError`。
  README 宣称支持 Seedance/Veo，实际调用必抛异常。

顺带：`execution: {retry: 3, timeout: 300}` 写在配置里，但引擎 `execute()` 里
**没有任何重试或超时逻辑**（`:110-145`），`self.config.execution` 从头到尾没被读过。

### 结论 3：ComfyUI 工作流模板是坏的，不是「没接好」而是「接错了」

`backend/config/comfyui_workflows/` 三个模板：

| 文件 | 问题 |
|---|---|
| `character-sheet.json` | **没有 IP-Adapter**。就是个裸 SD1.5 txt2img（CheckpointLoaderSimple → CLIPTextEncode ×2 → EmptyLatentImage → KSampler → VAEDecode → SaveImage），提示词硬编码 `"full body character reference sheet..."`，**连一个 `{{}}` 占位符都没有**。所谓「IP-Adapter 保证角色一致性」是 README 的说法，代码里不存在。 |
| `image-to-video.json` | 13 节点，有 `{{positive_prompt}}` / `{{reference_image}}` / `{{seed}}`。但**缺少 `WanImageToVideo` 节点**——`LoadImage` 后直接 `VAEEncode` 喂给 `SamplerCustomAdvanced`，Wan 2.2 I2V 必需的长度/尺寸/批次参数全部丢失。对照 OpenMontage 的同款工作流 `wan22-i2v-4step.json`（17 节点）包含 `WanImageToVideo`(#98)、`CreateVideo`(#94)、`ModelSamplingSD3`(#103/104)、`LoraLoaderModelOnly`(#101/102)，你的版本是残缺拷贝。另外 `{{seed}}` 经 `_render()` (`:177`) 替换后是**字符串**，ComfyUI 会拒绝字符串 seed。 |
| `scene-generation.json` | 同为 7 节点骨架。 |

---

## 一、缺口清单（按优先级）

### P0 —— 阻断端到端，必须先修

| # | 缺口 | 位置 | 现状 |
|---|---|---|---|
| 1 | `image` 节点类型未注册 | `engine.py:150` handler 表 | 工作流第 5 段直接崩 |
| 2 | seedance / veo 是空壳 | `video_providers.py:194,220-221` | 第 6 段直接崩 |
| 3 | ComfyUI 工作流缺 `WanImageToVideo` | `image-to-video.json` | 即使 ComfyUI 通了也出不了视频 |
| 4 | 角色图无 IP-Adapter / 无参数化 | `character-sheet.json` | 角色一致性完全靠不住 |
| 5 | 本地无 ComfyUI 实例 | 全盘无 `models/`、`custom_nodes/` | `.env` 指向 `localhost:8188`，但没人监听 |

### P1 —— 你要的「可控可视可改可参照」，一项不落全缺

| # | 缺口 | 现状 |
|---|---|---|
| 6 | **可视化编排画布** | `frontend/app/workflows/page.tsx` 仅 53 行，是列表页不是编辑器。README 宣称的 "Workflow Editor" 模式不存在 |
| 7 | **进度回传** | 后端无 SSE / WebSocket 推送。引擎 `get_status()` 有状态但没有任何 API 暴露，前端全程黑盒等待 |
| 8 | **本地资产库** | 无参考图 / 参考视频池。无法「挂本地图做参考」，只能传 URL |
| 9 | **批量与重试** | 配置写了 `retry: 3`，引擎没实现；无并发队列、无断点续跑 |
| 10 | **参数面板** | 提示词、seed、步数、CFG、尺寸、LoRA 权重均无 UI 可调，只能手改 JSON |
| 11 | **提示词管理** | 4 个 yaml 是写死的，不可在界面上编辑 / 复制 / 版本对比 |

### P2 —— 成片质量

| # | 缺口 | 现状 |
|---|---|---|
| 12 | TTS 配音 | 全项目 0 命中 |
| 13 | BGM / 音效 | 0 命中 |
| 14 | 字幕烧录 | 0 命中 |
| 15 | 转场与音轨混合 | `ffmpeg` 节点只做 `-c copy` 硬拼接，无转场、无音轨 |

---

## 二、成熟方案调研结论

调研了工作区内 17 个项目 + 业界公开实践，收敛出三条共识。

### 2.1 流水线分层是七段，不是三段

你现在的 `standard-drama.json` 是 7 个节点但缺了「角色资产」和「场景资产」两个独立的资产化阶段，直接从剧本跳到分镜再跳到生图。业界成熟方案（AIComicBuilder、huobao-drama-ai、OpenMontage）全部把**资产阶段独立出来**：

```
剧本 → 角色资产 → 场景资产 → 分镜 → 生图 → 生视频 → 后期
         ↑            ↑
    这两段决定成败，且只需生成一次，全剧复用
```

资产化的经济学意义：业界数据是第一部投入资产建设成本，第二部复用后边际成本下降 40–50%。你现在每集从头生成，等于每次都重建资产。

### 2.2 角色一致性是三层，不是一层

这是全行业最硬的问题，成熟方案一致采用三层叠加：

| 层 | 手段 | 解决什么 |
|---|---|---|
| 第一层 | **角色卡**（固定文本描述） | 保证语义编码路径一致 |
| 第二层 | **IP-Adapter**（参考图 → image embedding → 解耦交叉注意力注入） | 锁定脸型、肤色、光照等文本说不清的高维信息 |
| 第三层 | **角色 LoRA**（权重矩阵低秩偏移） | 解决极端角度 / 只剩轮廓时的身份衰减 |

你现在只有第一层（characters.yaml 文本），第二三层完全缺失。

### 2.3 同场景多镜头必须「条件共享」

一个场景多个镜头如果各自独立生成，风格必然接不上。成熟做法是把 prompt 拆成两部分：

- **不变部分**：场景描述、风格锚点、角色卡 —— 同场景多镜头完全一致
- **可变部分**：只换机位词和动作描述

同时 seed 用**递增策略**（seed、seed+1、seed+2）而非随机，让噪声场平滑过渡。
ControlNet OpenPose 需**每镜头独立生成骨架图**，共用骨架会导致「特写里主角还是走路姿势」这种翻车。

### 2.4 工作区内可直接复用的成熟代码

| 需求 | 直接抄哪个 | 位置 |
|---|---|---|
| ComfyUI 客户端（生产级） | **OpenMontage** | `OpenMontage-main/tools/_comfyui/client.py`（509 行） |
| ├ WS + 轮询双保险 | 同上 `:246 wait_ws` | 连 WS 前先探 history 消竞态，WS 超时回探 history，最终 fallback 轮询 |
| ├ 断点续等（超时不重跑） | 同上 `:411 resume_prompt_id` | 超时后拿 prompt_id 重连，**不重复烧 GPU** |
| ├ 模型/节点预检 | 同上 `:150 check_models` / `:163 has_node` | 提交前就知道缺什么 |
| └ 参考图上传 | 同上 `:393 upload_image` | POST /upload/image |
| 可视化节点画布 | **xiakeman-ai-short-drama** | `src/features/canvas/`（`nodeRegistry.ts`、`DataFlowEdge.tsx`、`useUpstreamNodes.ts`、`referenceImagePool.ts`），依赖 `@xyflow/react ^12.11.1`。**注意：PolyForm-Noncommercial 协议，禁止商用，只能参考不能抄代码** |
| 一致性引擎参考 | **AIComicBuilder-main** | `src/lib/pipeline/` 11 个模块 + `continuity-check.ts`，Apache-2.0 可商用 |
| 可用 Wan 2.2 I2V 工作流 | **OpenMontage** | `tools/_comfyui/workflows/wan22-i2v-4step.json`（17 节点，正确） |

### 2.5 ComfyUI 接入的三种路线

| 方案 | 适配 | 说明 |
|---|---|---|
| **comfy-python-sdk**（工作区已有） | ❌ 本机 | 打的是 **Comfy API v2**（`/api/v2/`、SSE），**不是**原生 `/prompt` + `/history` + ws。接原生 8188 全部 404。除非装 `comfy-api-proxy` |
| **ComfyKit**（工作区已有 zip） | ✅ 推荐 | 面向**原生本地 ComfyUI**，零配置。亮点是 **title-DSL 参数化**：在节点 `_meta.title` 里写 `$param` / `$param!`（必需）/ `$~param`（媒体自动上传）/ `$output.name`，比 `{{}}` 更规范且带类型语义 |
| **OpenMontage client.py** | ✅ 最成熟 | 直接可用，含上面列的全部工程细节 |

**推荐组合**：OpenMontage `client.py` 做底座 + ComfyKit 的 title-DSL 做参数声明 + 自己补批量队列与 SSE 回传。

---

## 三、补齐方案（分四阶段）

### 阶段 A · 止血（让现有链路能跑通）

1. 引擎 handler 表补 `image` 和 `comfyui_image` 的映射，或把 `standard-drama.json` 的 `image_gen` 改成 `type: "comfyui_image"`。
2. `video_providers.py` 删掉 `seedance` / `veo` 的假注册，或接真实 API。宁可只留 `agnes` + `kling`，也不要留会抛异常的占位。
3. 引擎 `execute()` 实现 `execution.retry` 和 `timeout`（现在是死配置）。
4. 修 `image-to-video.json`：补 `WanImageToVideo` / `ModelSamplingSD3`，`{{seed}}` 渲染时转 int。
5. 重写 `character-sheet.json`：加 IP-Adapter 节点 + 参数化占位符。

### 阶段 B · 资产化（决定成片质量上限）

6. 建**角色资产卡**：文本卡（固定描述）+ 三视图/四视图参考图 + IP-Adapter 权重 + seed 锁定，可复用、可版本对比。
7. 建**场景资产卡**：同上，附风格锚点。
8. 实现**条件共享**生成器：prompt = 不变部分（场景+风格+角色卡）+ 可变部分（机位+动作），seed 递增。
9. 本地**资产库**：参考图 / 参考视频池，支持拖入本地文件、打标签、在分镜上挂载。

### 阶段 C · 可视化编排（你要的「可控可视可改」）

10. 引入 `@xyflow/react` 建节点画布，**参考** xiakeman 的 canvas 结构（不抄代码，协议不允许商用）。
11. 画布节点类型：`剧本 / 角色卡 / 场景卡 / 分镜 / 生图 / 生视频 / 配音 / 合成`，每个节点右侧参数面板可改提示词、seed、步数、CFG、尺寸、LoRA、参考图挂载。
12. 画布 ⇄ `standard-drama.json` 双向序列化：画布改完能存回配置，配置能还原成画布。
13. 单节点重跑：改完参数只重跑这一个节点，下游缓存复用。

### 阶段 D · 进度与成片

14. SSE 进度回传：引擎每步推 `node_id / status / progress / preview`，前端实时显示 + 中间图预览。
15. 批量队列：并发控制、失败重试、断点续跑（复用 `resume_prompt_id` 思路）。
16. 后期：TTS 配音 + BGM + 字幕烧录 + 转场，ffmpeg 节点从 `-c copy` 升级为完整滤镜链。

---

## 四、需要你拍板的三件事

1. **算力放哪**——这决定整个架构：
   - (a) 全部走云 API（Kling / Seedance / Agnes / 即梦）：最快出片，但可控性弱、按次付费、参考图要传公网
   - (b) 远程 ComfyUI（云 GPU 主机或 RunningHub）：可控性最强，工作流随便改、随便挂 LoRA/IP-Adapter，但要租卡
   - (c) 混合：资产图走远程 ComfyUI（要一致性），视频走云 API（省事）

2. **继续修 `ai-drama-studio`，还是换基座**——`ai-drama-studio` 的 DAG 引擎和审批门设计不错（452 行，是项目里最健康的资产），但只有 2908 行后端 + 1458 行前端，缺的量很大。
   备选：`AIComicBuilder-main`（Apache-2.0 可商用，11 个流水线模块完整，`continuity-check.ts` 现成）。

3. **商用还是自用**——涉及协议：xiakeman 是 PolyForm-Noncommercial（禁商用），ai_story-main 是 CC BY-NC-SA（禁商用），AIComicBuilder / Jellyfish / OpenMontage 是 Apache-2.0（可商用）。如果打算商用，参考对象要避开前两个。
